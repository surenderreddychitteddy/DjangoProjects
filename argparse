l=["a","a_b","a b ()"]
import argparse
parser = argparse.ArgumentParser(description='Parse input string')
parser.add_argument('-d', help='Input String', nargs='+' ,choices=l)
args = parser.parse_args()
arg_str = ' '.join(args.d)
print(arg_str)

